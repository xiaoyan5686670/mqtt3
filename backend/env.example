# MQTT IoT管理系统 - 后端环境变量配置示例
# 复制此文件为 .env 并根据实际环境修改配置

# ==================== 应用基础配置 ====================
APP_NAME="MQTT IoT管理系统"
APP_VERSION="1.0.0"
DEBUG=false

# ==================== 安全配置 ====================
# ⚠️ 重要：生产环境必须修改此密钥！
# 生成方式: openssl rand -hex 32
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32

# JWT认证配置
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== 数据库配置 ====================
# SQLite数据库路径（相对路径或绝对路径）
DATABASE_URL=sqlite:///./mqtt_iot.db

# ==================== MQTT默认配置 ====================
MQTT_DEFAULT_PORT=1883
MQTT_DEFAULT_TIMEOUT=60
MQTT_KEEPALIVE=60

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=logs/app.log

# 日志文件轮转配置（单位：字节）
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5

# ==================== 服务器配置 ====================
HOST=0.0.0.0
PORT=8000

# ==================== CORS配置 ====================
# 允许的前端源地址，多个地址用逗号分隔
# 例如: http://localhost:5173,http://localhost:3000,http://192.168.1.103:5173
# 如果不设置此变量，将使用默认值（包含常见的前端地址）
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://192.168.1.103:5173