# STM32_4 温度监控模块说明

## 功能概述

为设备 `stm32_4` 创建了一个专属的温度监控模块，实时展示4个温度传感器的数据，并通过动态图标和颜色变化直观显示温度状态。

## 监控的温度字段

该模块监控以下4个温度传感器（华氏度）：

1. `comp1_in_temperature_F` - 压缩机1入口温度
2. `comp2_in_temperature_F` - 压缩机2入口温度
3. `comp2_out_temperature_F` - 压缩机2出口温度
4. `comp1_out_temperature_F` - 压缩机1出口温度

## 功能特性

### 1. 温度正常状态
- **温度阈值**：< 85°F (约29.4°C)
- **风扇图标**：持续旋转动画
- **颜色**：绿色主题
- **卡片边框**：绿色
- **状态标签**：显示"✓ 正常"

### 2. 温度异常状态
- **温度阈值**：≥ 85°F
- **风扇图标**：停止旋转
- **颜色**：红色主题（温度值以红色显示并闪烁）
- **卡片边框**：红色，带呼吸动画
- **状态标签**：显示"⚠ 异常"

### 3. 视觉效果
- **风扇旋转动画**：正常状态下，风扇图标以2秒一圈的速度持续旋转
- **温度闪烁**：异常状态下，温度数值以1.5秒周期闪烁
- **卡片呼吸**：异常状态下，卡片阴影以2秒周期呼吸动画
- **渐变背景**：
  - 正常：淡绿色渐变
  - 异常：淡红色渐变

### 4. 响应式设计
- **超大屏幕** (≥1200px)：4列布局，每个温度占1/4宽度
- **大屏幕** (≥992px)：2列布局
- **中屏幕** (≥768px)：2列布局
- **小屏幕** (<768px)：单列布局，自动堆叠

## 实现细节

### 设备识别
模块会自动识别以下任一条件的设备：
- 设备名称 (`device.name`) 为 `stm32_4`
- 设备显示名称 (`device.display_name`) 包含 `stm32_4`（不区分大小写）

### 数据刷新
- 自动刷新频率：**2秒**
- 与仪表板其他设备数据同步刷新

### 温度阈值自定义
如需调整温度阈值，修改 `Dashboard.vue` 中的 `isTempNormal` 函数：

```javascript
const isTempNormal = (sensor) => {
  if (!sensor || sensor.value === null || sensor.value === undefined) return false
  // 华氏度正常范围：修改这里的数值（当前为 85°F）
  return sensor.value < 85
}
```

## 使用说明

1. **访问仪表板**：打开 http://192.168.1.102:5173/dashboard
2. **查看监控模块**：温度监控模块会自动显示在页面顶部（设备卡片网格上方）
3. **实时监控**：
   - 观察风扇图标是否旋转
   - 查看温度数值和颜色
   - 注意状态标签（正常/异常）

## 注意事项

1. **设备必须存在**：如果设备 `stm32_4` 不在线或不存在，监控模块不会显示
2. **传感器类型匹配**：确保传感器类型名称完全匹配上述4个字段名
3. **温度单位**：默认为华氏度 (°F)，如需更改请修改模板中的单位显示

## 文件修改

修改的文件：
- `/frontend/src/views/Dashboard.vue` - 添加温度监控模块

新增内容：
1. **模板**：添加 STM32_4 温度监控专区 HTML 结构
2. **脚本**：
   - `stm32Device` 计算属性：查找 stm32_4 设备
   - `stm32TemperatureSensors` 计算属性：过滤并排序温度传感器
   - `isTempNormal` 方法：判断温度是否正常
   - `getTempCardClass` 方法：获取卡片样式类
3. **样式**：
   - 温度卡片样式
   - 风扇旋转动画
   - 温度异常闪烁效果
   - 卡片呼吸动画
   - 响应式布局

## 技术栈

- **Vue 3** - 响应式框架
- **CSS3 动画** - 风扇旋转、闪烁、呼吸效果
- **Font Awesome** - 图标库（风扇图标）
- **Bootstrap 5** - 响应式网格布局

## 效果预览

### 正常状态
```
┌────────────────────────────────────┐
│  🌀 压缩机1入口温度                 │
│  75.0 °F                          │
│  ✓ 正常                           │
└────────────────────────────────────┘
   (绿色边框，风扇旋转)
```

### 异常状态
```
┌────────────────────────────────────┐
│  🛑 压缩机1入口温度                 │
│  90.0 °F (红色闪烁)                │
│  ⚠ 异常                           │
└────────────────────────────────────┘
   (红色边框呼吸，风扇停止)
```

## 后续优化建议

1. **温度阈值配置化**：将温度阈值存储在数据库或配置文件中，支持动态调整
2. **历史趋势图**：添加温度历史数据的趋势图表
3. **报警通知**：温度异常时发送邮件/短信/系统通知
4. **声音提示**：温度异常时播放警报声音
5. **多级阈值**：设置警告(warning)和危险(danger)两个阈值级别
6. **数据导出**：支持导出温度历史数据到 CSV/Excel
